/**
 * Splash brand mark — replace with your asset (e.g. put `logo.svg` in `public/` and set `src: '/logo.svg'`).
 * Leave `src` empty to use the built-in mark in `SplashScreen`.
 */
export const SPLASH_BRAND_LOGO: { src: string; alt: string } = {
  src: '',
  alt: 'Theria: Finance',
};
