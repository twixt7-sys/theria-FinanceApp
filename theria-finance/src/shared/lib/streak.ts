/**
 * Single source for the current streak length. The value is still a placeholder
 * (no streak tracking backend yet), but keeping it in one place means the
 * top-nav badge, the profile menu, the profile screen, and the streak screen
 * can never disagree about the number they show.
 */
export const CURRENT_STREAK_DAYS = 7;
